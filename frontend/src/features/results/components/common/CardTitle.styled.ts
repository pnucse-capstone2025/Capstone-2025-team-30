import styled from "styled-components";

/**
 * 카드 제목 스타일 컴포넌트
 * Results 페이지의 카드들에서 사용하는 제목 스타일을 제공
 */
const CardTitle = styled.div`
  font-size: 24px;
  color: rgb(12, 12, 13);
  margin-bottom: 6px;
  font-weight: 600;
  font-family: 'Noto Sans KR', sans-serif;
  font-style: bold;
`;

export default CardTitle;
