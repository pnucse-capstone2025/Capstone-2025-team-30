import styled from "styled-components";

/**
 * 페이지 레이아웃 스타일 컴포넌트
 * Results 페이지의 전체 레이아웃을 정의
 */
const Page = styled.div`
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 24px;
`;

export default Page;